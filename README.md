# Skeleton project for Swagger
# express as server 
# packages: swagger-express-mw,nodemon
# to run the application: go to the direction of the project which you did clone and then run "nodemon", if everything was ok you will see this message "server is running on http://localhost:10010
# the path of the codes files are /api/controllers/complexityService.js and /api/controllers/complexity
# one API is POST Method :http://localhost:10010/complexity 
# POST Method has a body for get request from body
# {
#	"str":"Kim loves going to the cinema",
#	"strGreen": "to the"
# }
# The second API is GET with this path http://localhost:10010/complexity?mode=the
